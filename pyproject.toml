[project]
name = "asr-summarizer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "gradio",
    "torch",
    "torchaudio",
    "transformers",
]
